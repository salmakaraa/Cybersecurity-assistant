<div class="users-header">
    <h1>Users Management</h1>
    <a href="/create-admin" class="btn-create">➕ Add New Admin</a>
</div>

<h2>Admins</h2>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Verified</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
            {% if 'ROLE_ADMIN' in user.roles %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles|join(', ') }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('admin_user_delete', {id: user.id}) }}"
                       onclick="return confirm('Are you sure you want to delete this admin?')"
                       class="btn-delete">🗑️ Delete</a>
                </td>
            </tr>
            {% endif %}
        {% else %}
            <tr>
                <td colspan="5">No admins found.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<h2>Users</h2>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Verified</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
            {% if 'ROLE_ADMIN' not in user.roles %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles|join(', ') }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('admin_user_delete', {id: user.id}) }}"
                       onclick="return confirm('Are you sure you want to delete this user?')"
                       class="btn-delete">🗑️ Delete</a>
                </td>
            </tr>
            {% endif %}
        {% else %}
            <tr>
                <td colspan="5">No users found.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<style>
.table {
    width: 100%;
    border-collapse: collapse;
    background: rgba(255,255,255,0.03);
    border-radius: 15px;
    margin-bottom: 40px;
}

.table th, .table td {
    padding: 15px 12px;
    text-align: left;
    font-size: 14px;
}

.table thead {
    background: rgba(255,255,255,0.06);
}

.btn-create {
    display: inline-block;
    margin-bottom: 20px;
    padding: 10px 18px;
    border-radius: 12px;
    font-weight: 600;
    font-size: 14px;
    color: #fff;
    text-decoration: none;
    border: none;
    background: linear-gradient(135deg, #57ff8a, #2cbf5f);
    cursor: pointer;
}

.btn-create:hover {
    transform: scale(1.05);
    box-shadow: 0 6px 12px rgba(87,255,138,0.5);
}

.btn-delete {
    padding: 8px 14px;
    border-radius: 10px;
    color: #fff;
    text-decoration: none;
    border: none;
    background: linear-gradient(135deg, #ff4d4d, #d93636);
    cursor: pointer;
}

.btn-delete:hover {
    transform: scale(1.05);
    box-shadow: 0 6px 12px rgba(255,77,77,0.5);
}
</style>